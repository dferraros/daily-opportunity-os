# Weekly Opportunity Review — {{ week }}
*{{ date_range }}*

---

## 1. Top 3 to Validate This Week

{% for opp in promote[:3] %}
### {{ loop.index }}. {{ opp.name }} ({{ opp.geography }})
- **Score:** {{ opp.final_score | default(0) | round(1) }}/10 | **Lane:** `{{ opp.portfolio_lane }}`
- **Test this week:** {{ opp.next_test | default('Define a specific test → update opportunity record') }}
- **Expected signal:** {{ opp.validation_notes | default('Revenue interest or clear rejection from 5+ contacts') }}
{% endfor %}

---

## 2. Top 3 to Kill This Week

{% for opp in kill[:3] %}
### ~~{{ opp.name }}~~
- **Kill reason:** {{ opp.kill_reason | default('Failed kill gate — see criteria') }}
- **What we learned:** {{ opp.kill_notes | default('') }}
{% endfor %}

---

## 3. Rising Signals

{% for r in rising[:3] %}
- **{{ r.name }}** — {{ r.prev_score | round(1) }} → {{ r.final_score | round(1) }} (+{{ r.delta | round(1) }}) — *{{ r.rise_reason | default('score updated') }}*
{% endfor %}

---

## 4. Conviction Area (This is ONE thing, not a list)

> **{{ conviction_area | default('Not set — run weekly-review skill') }}**

*This is the sector/geography/archetype to double down on for the next 30 days.*

---

## Weekly Quota Check

| Metric | Target | Actual | Status |
|--------|--------|--------|--------|
| Raw signals ingested | 30–50 | {{ quota_status.signals | default('?') }} | {{ '✅' if quota_status.signals_ok else '❌' }} |
| Structured opportunities | 10 | {{ quota_status.structured | default('?') }} | {{ '✅' if quota_status.structured_ok else '❌' }} |
| Deep dives produced | 3 | {{ quota_status.deep_dives | default('?') }} | {{ '✅' if quota_status.deep_dives_ok else '❌' }} |
| Validation runs | 1–2 | {{ quota_status.validations | default('?') }} | {{ '✅' if quota_status.validations_ok else '❌' }} |

---
*Weekly review — daily-opportunity-os · {{ week }}*
