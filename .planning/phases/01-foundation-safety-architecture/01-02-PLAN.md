---
phase: 01-foundation-safety-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation-safety-architecture/playbook-section-suppression.md
  - .planning/phases/01-foundation-safety-architecture/playbook-section-event-schema.md
  - .planning/phases/01-foundation-safety-architecture/playbook-section-hightouch.md
autonomous: true
requirements: [FOUND-03, FOUND-04, FOUND-05]

must_haves:
  truths:
    - "Anyone reading the playbook knows exactly how C8 whale users are suppressed and how the CSV is uploaded and refreshed"
    - "Anyone reading the playbook can identify quiet hours, opt-out handling, and escalating cooldown rules"
    - "An engineer reading the event schema section can implement all required CleverTap events without asking clarifying questions"
    - "Alvaro can set up the Hightouch BigQuery-to-CleverTap sync from the playbook alone -- credentials, source view SQL, field mapping, schedule"
  artifacts:
    - path: ".planning/phases/01-foundation-safety-architecture/playbook-section-suppression.md"
      provides: "Suppression system design covering C8, quiet hours, opt-out, cooldown escalation"
      contains: "C8_Whale_Suppression"
    - path: ".planning/phases/01-foundation-safety-architecture/playbook-section-event-schema.md"
      provides: "Event schema with SDK events, Backend Upload events, reserved names, property constraints"
      contains: "Upload Events API"
    - path: ".planning/phases/01-foundation-safety-architecture/playbook-section-hightouch.md"
      provides: "Hightouch Reverse ETL integration design with BigQuery source, CleverTap destination, field mapping"
      contains: "user_profiles_for_clevertap"
  key_links:
    - from: "playbook-section-suppression.md"
      to: "playbook-section-hightouch.md"
      via: "Suppression flags synced from BigQuery to CleverTap via Hightouch"
      pattern: "suppression_flags"
    - from: "playbook-section-event-schema.md"
      to: "playbook-section-hightouch.md"
      via: "Events tracked feed into user profile attributes synced by Hightouch"
      pattern: "notification_fatigue_score"
---

<objective>
Write the Suppression System, Event Schema, and Hightouch Reverse ETL Integration Design sections of the playbook.

Purpose: These three sections define HOW unwanted sends are blocked (suppression), WHAT data flows into the system (events), and HOW BigQuery-computed attributes reach CleverTap (Reverse ETL). Together with Plan 01 (Preference Center + Frequency Caps), they complete the safety architecture foundation.

Output: Three playbook sections (Markdown documents) that complete Phase 1's five FOUND requirements.
</objective>

<execution_context>
@C:/Users/ferra/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ferra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/phases/01-foundation-safety-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Suppression System Section (FOUND-03)</name>
  <files>.planning/phases/01-foundation-safety-architecture/playbook-section-suppression.md</files>
  <read_first>
    - .planning/phases/01-foundation-safety-architecture/01-RESEARCH.md (Pattern 3: Suppression via Custom List Segments + DND, 4 suppression layers, Custom List API workflow)
    - .planning/research/PITFALLS.md (Pitfall 7: anti-fatigue formula cooldowns; Pitfall 8: quiet hours 22:00-08:00; Pitfall 12: scam-lookalike; Anti-Pattern 4: dismissed signals)
    - .planning/research/ARCHITECTURE.md (Component 4: Suppression and Fatigue Layer, suppression segment types)
    - .planning/research/STACK.md (CleverTap API constraints: 15 concurrent requests, Custom List CSV format)
  </read_first>
  <action>
Write a complete playbook section titled "## 3. Suppression System" containing:

**3.1 Suppression Overview** -- Brief intro (3-4 sentences) explaining:
- Suppression prevents high-risk sends: whale accounts getting generic blasts, users receiving pushes at 3 AM, opted-out users getting marketing
- Four suppression layers operate in sequence: Segment Exclusion, Quiet Hours (DND), Opt-Out Handling, Escalating Cooldowns
- Suppression is ADDITIVE -- a notification must pass ALL layers to send

**3.2 Segment-Based Suppression** -- Table with 5 columns:
| Segment Name | Source | Users | Refresh Cadence | CleverTap Mechanism |
Include these exact suppression segments:
- C8_Whale_Suppression: BigQuery query (high-balance dormant users, definition from Alvaro), ~TBD users (pending Alvaro query), weekly refresh, Custom List segment via CSV upload or Custom List API
- Excluded_Users: BigQuery (lifecycle_stage = EXCLUDED), ~600,000 users, daily refresh, Custom List segment
- Active_Support_Tickets: Backend/CRM system (users with open support tickets), variable, daily refresh, Custom List segment
- Legal_Hold: Diego-flagged users (pending compliance review), variable, on-demand, Custom List segment
- Notification_Cooldown: BigQuery (users with notification_cooldown = true from fatigue system), variable, daily via Hightouch sync, User property filter in campaign targeting

For C8_Whale_Suppression, include the full upload workflow:
1. Alvaro runs BigQuery query to generate C8 user_id list
2. Export as CSV with format: Type,Identity (header) then i,user_id per row
3. Upload via CleverTap dashboard (< 50MB) or Custom List API (< 5GB):
   - API Step 1: POST /custom-list/url with body {"name": "C8_Whale_Suppression"} to get pre-signed URL
   - API Step 2: PUT CSV to pre-signed URL
   - API Step 3: POST /custom-list/upload-done with body {"name": "C8_Whale_Suppression"}
4. Verify: Check segment size in CleverTap dashboard matches BigQuery count
5. Schedule: Every Monday morning (Katy) or automate via Cloud Function calling Custom List API

State the BLOCKER: "C8 suppression CSV is currently NOT uploaded to CleverTap (identified in LC-OS Phase A audit, March 2026). This is a P0 action item before ANY marketing campaign launches."

**3.3 Quiet Hours (DND)** -- Specification:
| Parameter | Value | Notes |
- Quiet window: 22:00 - 08:00 user local timezone
- Timezone source: CleverTap device timezone (auto-detected from SDK)
- Fallback timezone: CET/CEST (Spain) if user timezone unknown
- DND action: DELAY delivery (queue until 08:00), not DISCARD
- Exception: P0 Security notifications (login from new device, withdrawal confirmation, 2FA) are NEVER subject to DND
- CleverTap config: Per campaign > Settings > When > Delivery Preferences > DND checkbox

State: "DND is configured PER CAMPAIGN in CleverTap, not globally. Every new campaign must have DND configured. This is item #4 on the Campaign Creation Checklist (see Section 2.6 Frequency Cap Policy)."

Include timezone-specific quiet hours table:
| Region | Quiet Start | Quiet End | Timezone |
- Spain: 22:00 | 08:00 | CET (UTC+1) / CEST (UTC+2)
- LatAm (general): 22:00 | 08:00 | User local timezone
- Central Europe: 22:00 | 08:00 | CET/CEST
- Note: CleverTap resolves per-user timezone automatically from device. These are the expected ranges.

**3.4 Opt-Out Handling** -- Per-channel specification:
| Channel | Opt-Out Mechanism | System Response | Reversibility | CleverTap Property |
- Push: User disables in iOS Settings / Android app settings -- MSG-push set to false, removed from ALL push campaigns -- Requires user to manually re-enable in device settings (effectively permanent on iOS) -- MSG-push
- Push (granular): User turns off a category in-app preference center -- consent_marketing_push or specific category flag set to false in BigQuery, synced to CleverTap -- Reversible via preference center -- Custom user property
- Email: User clicks unsubscribe link in email -- CleverTap Subscription Group unsubscribed, "Channel Unsubscribed" event fired -- Reversible via re-subscribe or preference center -- Subscription Group status
- Email (global): User clicks "Unsubscribe from all" -- MSG-email set to false -- Reversible via preference center -- MSG-email
- In-App: User closes/dismisses -- No permanent opt-out for in-app (channel does not have permission model) -- N/A -- Dismissed count tracked for fatigue

State the audit requirement: "All opt-out events must be logged in BigQuery (bit2me_lifecycle.notification_log) with timestamp, channel, category, and reason. GDPR Article 7(3): withdrawing consent must be as easy as giving it."

**3.5 Escalating Dismissal Cooldowns** -- Table with escalation ladder:
| Level | Trigger Condition | Cooldown Applied | Scope | Duration | Reset Condition |
- Level 0: Normal -- no dismissals -- no cooldown -- -- -- --
- Level 1: 1 dismissal of a notification -- 24-hour cooldown for that trigger family -- per user per family -- 24 hours -- Auto-expires after 24h
- Level 2: 3 consecutive dismissals of same trigger family -- 7-day cooldown for that family -- per user per family -- 7 days -- Auto-expires OR user clicks a notification from that family
- Level 3: 5 consecutive dismissals across ANY trigger families -- Reduce to P0-P1 only -- per user (all families) -- 14 days -- User opens/clicks any notification
- Level 4: 10+ dismissals in 30 days -- Flag for manual review, consider permanent suppression from P3+ -- per user -- Until manual review -- Katy reviews and decides

State: "Escalation levels are computed daily in BigQuery from bit2me_lifecycle.notification_log (dismissal events). Level 3+ sets notification_cooldown = true in user_profiles, synced to CleverTap via Hightouch. All P3-P5 campaigns must include targeting filter: notification_cooldown != true."

**3.6 Campaign Launch Suppression Checklist** -- Numbered checklist:
1. C8_Whale_Suppression segment added to exclusion? YES/NO
2. Excluded_Users segment added to exclusion? YES/NO
3. Active_Support_Tickets segment added to exclusion? YES/NO
4. DND configured (22:00-08:00, delay, all days)? YES/NO
5. notification_cooldown != true in targeting? YES/NO
6. fatigue_risk < threshold for this tier? YES/NO (see Section 2.5)
7. Diego copy approval obtained? YES/NO
8. If targeting dormant users: warm-up schedule defined? YES/NO

State: "ALL items must be YES before campaign activation. Katy owns this checklist. Any NO = campaign blocked until resolved."
  </action>
  <verify>
    <automated>python3 -c "
import os
path = 'C:/Users/ferra/OneDrive/Desktop/.planning/phases/01-foundation-safety-architecture/playbook-section-suppression.md'
content = open(path).read()
checks = [
    ('C8_Whale_Suppression' in content, 'Has C8 whale suppression segment'),
    ('Excluded_Users' in content, 'Has excluded users segment'),
    ('Custom List' in content, 'References Custom List API'),
    ('22:00' in content and '08:00' in content, 'Has quiet hours spec'),
    ('DND' in content, 'Has DND configuration'),
    ('MSG-push' in content, 'Has MSG-push opt-out property'),
    ('Level 1' in content and 'Level 3' in content, 'Has escalation levels'),
    ('notification_cooldown' in content, 'Has cooldown flag'),
    ('Diego' in content, 'References Diego legal gate'),
    ('LC-OS' in content or 'audit' in content.lower(), 'References C8 blocker from audit'),
    (content.count('|') > 20, 'Has multiple tables'),
    ('pre-signed' in content or 'presigned' in content, 'Has Custom List API workflow'),
]
passed = sum(1 for c, _ in checks if c)
for c, label in checks:
    status = 'PASS' if c else 'FAIL'
    print(f'  {status}: {label}')
print(f'Result: {passed}/{len(checks)} checks passed')
assert passed >= 10, f'Only {passed}/12 checks passed'
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Section contains heading "## 3. Suppression System"
    - Segment-Based Suppression table lists 5 segments: C8_Whale_Suppression, Excluded_Users, Active_Support_Tickets, Legal_Hold, Notification_Cooldown
    - C8 upload workflow documented with 5 steps including Custom List API (POST /custom-list/url, PUT CSV, POST /upload-done)
    - C8 blocker explicitly stated: CSV not currently uploaded, P0 action item
    - Quiet Hours specified: 22:00-08:00 user local timezone, DELAY not DISCARD, P0 exempt
    - Timezone table with Spain, LatAm, Central Europe
    - Opt-Out Handling table covers Push, Push (granular), Email, Email (global), In-App
    - Escalating Cooldowns table has 5 levels (0-4) with trigger conditions, durations, reset conditions
    - Campaign Launch Suppression Checklist has 8 items
    - Cross-references Section 2.5 (Fatigue) and Section 2.6 (Campaign Checklist)
  </acceptance_criteria>
  <done>Suppression system section is complete with C8 whale workflow, quiet hours, opt-out handling per channel, escalating cooldowns, and campaign checklist. Any campaign manager can verify suppression compliance from this document.</done>
</task>

<task type="auto">
  <name>Task 2: Write Event Schema Section (FOUND-04)</name>
  <files>.planning/phases/01-foundation-safety-architecture/playbook-section-event-schema.md</files>
  <read_first>
    - .planning/phases/01-foundation-safety-architecture/01-RESEARCH.md (Pattern 4: Event Schema, SDK vs Upload Events, property constraints, reserved names, Charged event)
    - .planning/research/ARCHITECTURE.md (Event Schema table, Event Ingestion Layer, minimum data requirements)
    - .planning/research/STACK.md (CleverTap API constraints, Upload Events API rate limits)
  </read_first>
  <action>
Write a complete playbook section titled "## 4. Event Schema" containing:

**4.1 Event Ingestion Architecture** -- Brief description (3-4 sentences):
- Two ingestion paths: CleverTap SDK (client-side, auto-tracked + custom events) and Upload Events API (server-side, backend events)
- SDK events reach CleverTap in real-time. Upload Events must arrive within 2 hours of occurrence for Live campaign triggers.
- All events also flow to BigQuery for scoring, attribution, and fatigue computation.

**4.2 SDK Events (Tracked in App)** -- Table with 5 columns:
| Event Name | Properties | Property Types | Trigger Use | Notes |
Include these exact events:
- Product_Viewed: {product_name: string, duration_sec: integer} -- Cross-sell intent detection -- Auto-tracked if CleverTap SDK Page Viewed is enabled
- Price_Alert_Set: {asset: string, target_price: float, direction: string ("above"/"below")} -- User-configured alert activation -- direction must be exactly "above" or "below"
- Asset_Added_Watchlist: {asset: string} -- Market alert eligibility -- Tracks user interest signals
- Preference_Changed: {category: string, channel: string, new_status: boolean} -- Consent tracking -- Fires when user toggles notification preference
- App_Opened: {session_count: integer, days_since_last: integer} -- Engagement tracking, reactivation trigger -- Auto-tracked by CleverTap SDK
- Push_Permission_Changed: {new_status: boolean} -- Channel availability -- Auto-tracked by CleverTap SDK
- Notification_Clicked: {campaign_id: string, trigger_type: string, channel: string} -- Feedback loop, fatigue reset -- Auto-tracked by CleverTap SDK

**4.3 Backend Upload Events (via Upload Events API)** -- Table with same 5 columns:
| Event Name | Properties | Property Types | Trigger Use | Notes |
Include these exact events:
- Charged: {Amount: float, currency: string ("EUR"), Items: [{name: string, quantity: integer, price: float}]} -- Revenue tracking, purchase triggers -- MUST use "Charged" (CleverTap built-in for revenue/LTV). Do NOT use custom event names for purchases.
- Deposit_Completed: {amount_eur: float, method: string, is_first_deposit: boolean} -- Activation triggers, post-deposit nudge -- is_first_deposit enables onboarding journey trigger
- Withdrawal_Completed: {amount_eur: float, asset: string} -- Risk/churn signal -- Large withdrawals may trigger protective alerts
- KYC_Step_Completed: {step_number: integer, total_steps: integer} -- Onboarding nudges, abandonment detection -- Track each step for funnel analysis
- Earn_Subscribed: {asset: string, amount: float, apy: float} -- Product adoption tracking -- Cross-sell trigger (user adopted Earn)
- Order_Filled: {asset: string, amount_eur: float, side: string ("buy"/"sell"), product: string ("brokerage"/"pro")} -- Trading activity, frequency tracking -- Use alongside "Charged" for trade-specific properties

**4.4 Cloud Function Events (via Upload Events API)** -- Table:
| Event Name | Properties | Property Types | Trigger Use | Notes |
- Market_Trigger_Fired: {asset: string, change_pct: float, direction: string ("up"/"down"), trigger_type: string ("volatility"/"momentum"/"volume")} -- Proactive market alerts -- Cloud Function generates these on CoinGecko polling
- Price_Alert_Triggered: {asset: string, current_price: float, target_price: float, direction: string} -- User-configured price alert delivery -- Matches against user's Price_Alert_Set events

**4.5 Upload Events API Constraints** -- Table:
| Constraint | Value | Impact |
- Max records per API call: 1000 -- Batch uploads, do not send one event per call
- Max concurrent requests: 15 -- Queue with backpressure in Cloud Function / backend
- Max event types per account: 512 -- Plan event naming carefully, do not create variants
- Max properties per event: 256 -- More than sufficient for all events above
- Event name case sensitivity: Case-INSENSITIVE ("Purchase" and "purchase" are the same event) -- Standardize to Title_Case with underscores
- Prohibited characters in event names: % > < ! | & . : ; $ ' " \ -- Validate before sending
- Timestamp format: Unix epoch (seconds) -- Must be within 2 hours of current time for Live campaigns
- Test mode: dryRun=1 parameter -- Always test with dryRun before production upload

**4.6 Reserved Event Names (NEVER Use for Custom Events)** -- List:
- Notification Sent
- Notification Viewed
- Notification Clicked (system version -- but our custom Notification_Clicked with underscore is safe)
- App Launched
- App Installed
- UTM Visited
- Push Impressions
- Charged (RESERVED for purchase tracking -- use it, don't rename it)

State: "If a backend developer accidentally uses a reserved event name, CleverTap silently drops the event with no error. Always check this list before creating a new event type."

**4.7 Upload Events API Example** -- Include the JSON example:
```json
POST https://api.clevertap.com/1/upload
Headers: X-CleverTap-Account-Id: {ACCOUNT_ID}, X-CleverTap-Passcode: {PASSCODE}

{
  "d": [
    {
      "identity": "user_12345",
      "type": "event",
      "evtName": "Deposit_Completed",
      "evtData": {
        "amount_eur": 500.00,
        "method": "bank_transfer",
        "is_first_deposit": true
      },
      "ts": 1711152000
    }
  ]
}
```

**4.8 Event-to-Trigger Mapping** -- Table showing which triggers each event enables:
| Event | Enables Trigger Type | Trigger Family | Priority Tier |
- Charged -> First purchase celebration, repeat purchase nudge -> Behavioral/Lifecycle -> P2
- Deposit_Completed -> Post-deposit activation (no trade in 24h) -> Behavioral -> P2
- Withdrawal_Completed -> Large withdrawal protective alert -> Risk -> P0/P2
- KYC_Step_Completed -> KYC abandonment nudge (started, not finished in 48h) -> Lifecycle -> P2
- Product_Viewed -> Cross-sell intent (viewed Earn page 3x, never subscribed) -> Cross-sell -> P4
- Price_Alert_Set -> User-configured price alert activation -> User-Configured -> P1
- Market_Trigger_Fired -> Proactive market alert delivery -> Market -> P3
- App_Opened (after 7+ days) -> Reactivation welcome -> Lifecycle -> P2
  </action>
  <verify>
    <automated>python3 -c "
import os
path = 'C:/Users/ferra/OneDrive/Desktop/.planning/phases/01-foundation-safety-architecture/playbook-section-event-schema.md'
content = open(path).read()
checks = [
    ('Charged' in content, 'Has Charged event'),
    ('Upload Events API' in content, 'References Upload Events API'),
    ('Deposit_Completed' in content, 'Has Deposit_Completed event'),
    ('Market_Trigger_Fired' in content, 'Has Market_Trigger_Fired event'),
    ('dryRun' in content, 'Has dryRun test mode'),
    ('512' in content, 'Has max event types constraint'),
    ('1000' in content, 'Has max records per call'),
    ('reserved' in content.lower(), 'Has reserved event names section'),
    ('identity' in content, 'Has API example with identity'),
    ('Price_Alert_Set' in content, 'Has Price_Alert_Set SDK event'),
    (content.count('|') > 30, 'Has multiple tables'),
    ('Trigger Family' in content or 'trigger_type' in content, 'Has event-to-trigger mapping'),
]
passed = sum(1 for c, _ in checks if c)
for c, label in checks:
    status = 'PASS' if c else 'FAIL'
    print(f'  {status}: {label}')
print(f'Result: {passed}/{len(checks)} checks passed')
assert passed >= 10, f'Only {passed}/12 checks passed'
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Section contains heading "## 4. Event Schema"
    - SDK Events table lists 7 events with properties, types, trigger use, and notes
    - Backend Upload Events table lists 6 events including "Charged" with Items array
    - Cloud Function Events table lists 2 events (Market_Trigger_Fired, Price_Alert_Triggered)
    - API Constraints table with 8 constraints including max records (1000), concurrent requests (15), max event types (512)
    - Reserved Event Names list with 8+ names and warning about silent drop
    - JSON example of Upload Events API call with headers
    - Event-to-Trigger Mapping table showing which triggers each event enables with priority tier
    - Statement: "Charged" is mandatory for purchase tracking (CleverTap built-in revenue/LTV)
  </acceptance_criteria>
  <done>Event schema section documents all required events (SDK, Backend, Cloud Function), API constraints, reserved names, and event-to-trigger mapping. An engineer can implement all CleverTap event tracking from this document without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 3: Write Hightouch Reverse ETL Integration Design Section (FOUND-05)</name>
  <files>.planning/phases/01-foundation-safety-architecture/playbook-section-hightouch.md</files>
  <read_first>
    - .planning/phases/01-foundation-safety-architecture/01-RESEARCH.md (Pattern 5: Hightouch Reverse ETL, setup requirements, sync config, field mapping, key insights)
    - .planning/research/ARCHITECTURE.md (Data Flow: BigQuery to CleverTap sync, Pattern 2: Reverse ETL)
    - .planning/research/STACK.md (Hightouch Business tier, BigQuery source, CleverTap destination, Integration Architecture section)
  </read_first>
  <action>
Write a complete playbook section titled "## 5. Hightouch Reverse ETL Integration Design" containing:

**5.1 Why Reverse ETL** -- Brief section (3-4 sentences):
- BigQuery is the source of truth for user lifecycle stage, Health Score, fatigue score, segment membership, and product eligibility
- CleverTap needs these attributes as user properties to target campaigns (e.g., "Enter journey when health_score < 40 AND lifecycle_stage = AT_RISK")
- Hightouch syncs BigQuery-computed attributes to CleverTap user profiles with incremental change detection -- only changed rows are pushed
- Alternative approaches (custom ETL scripts, direct API calls) are fragile and lack error handling, alerting, and visual mapping

**5.2 Setup Requirements** -- Table:
| Requirement | Value | Owner | Notes |
- CleverTap Account ID: From CleverTap Project Settings | Katy | Required for Hightouch destination config
- CleverTap Passcode: From CleverTap Project Settings | Katy | API authentication
- CleverTap Region: EU (eu1.clevertap.com) | Katy | Bit2Me is EU-hosted
- BigQuery Service Account: Read access to bit2me_lifecycle schema | Alvaro | JSON key file for Hightouch source config
- Hightouch Account: Business tier ($350-500/month) | Growth team (budget approval) | Needed for scheduled syncs and multiple models

**5.3 BigQuery Source View** -- Full SQL:
```sql
CREATE OR REPLACE VIEW `bit2me_lifecycle.user_profiles_for_clevertap` AS
SELECT
  user_id,
  lifecycle_stage,
  health_score,
  segment_id,
  total_balance_eur,
  DATE_DIFF(CURRENT_DATE(), last_activity_date, DAY) AS days_since_last_activity,
  DATE_DIFF(CURRENT_DATE(), last_deposit_date, DAY) AS days_since_last_deposit,
  ARRAY_TO_STRING(products_active, ',') AS products_active,
  notification_fatigue_score,
  ARRAY_TO_STRING(suppression_flags, ',') AS suppression_flags,
  consent_marketing_push,
  consent_marketing_email,
  consent_marketing_inapp,
  country,
  preferred_language,
  space_center_tier,
  CASE WHEN notification_cooldown = TRUE THEN 'true' ELSE 'false' END AS notification_cooldown
FROM `bit2me_lifecycle.user_profiles`
WHERE lifecycle_stage != 'EXCLUDED'
```

State: "This view excludes EXCLUDED users (~600K) to reduce sync volume. Excluded users should never receive any notification and are handled by the Excluded_Users suppression segment. Alvaro creates this view. Hightouch references it as the sync model."

**5.4 Field Mapping Table** -- Table with 6 columns:
| BigQuery Column | CleverTap Property | Type | Description | Used By | Critical |
Map ALL fields:
- user_id -> Identity (CleverTap primary key) -> string -> Primary identifier -> All campaigns -> YES
- lifecycle_stage -> lifecycle_stage -> string -> REGISTERED, KYC, DEPOSITED, FM, ACTIVE, POWER, AT_RISK, PRE_DORMANCY, DORMANT_BAL, DORMANT_ZERO, REACTIVATED, CHURNED -> Lifecycle journey entry/exit -> YES
- health_score -> health_score -> integer (0-100) -> Health Score composite metric -> AT_RISK detection, fatigue gating -> YES
- segment_id -> segment_id -> string (SEG-01 to SEG-37) -> MECE segment membership -> Segment-specific campaigns -> YES
- total_balance_eur -> total_balance_eur -> float -> Total balance in EUR -> Whale detection, cross-sell eligibility -> YES
- days_since_last_activity -> days_since_last_activity -> integer -> Days since last app/trade activity -> Dormancy triggers -> YES
- days_since_last_deposit -> days_since_last_deposit -> integer -> Days since last deposit -> Funding nudge triggers -> NO
- products_active -> products_active -> string (comma-separated) -> Active product list -> Cross-sell eligibility -> YES
- notification_fatigue_score -> notification_fatigue_score -> float (0-1) -> Fatigue Risk Score from Section 2.5 -> P3-P5 campaign targeting filter -> YES
- suppression_flags -> suppression_flags -> string (comma-separated) -> Active suppression reasons (C8, EXCLUDED, etc.) -> Campaign exclusion -> YES
- consent_marketing_push -> consent_marketing_push -> boolean -> Push marketing consent -> Push campaign targeting -> YES
- consent_marketing_email -> consent_marketing_email -> boolean -> Email marketing consent -> Email campaign targeting -> YES
- consent_marketing_inapp -> consent_marketing_inapp -> boolean -> In-app marketing consent -> In-app campaign targeting -> NO
- country -> country -> string -> User country (ES, VE, etc.) -> Geo targeting, compliance rules -> NO
- preferred_language -> preferred_language -> string -> User language -> Template localization -> NO
- space_center_tier -> space_center_tier -> integer (1-7) -> Space Center gamification tier -> Gamification triggers -> NO
- notification_cooldown -> notification_cooldown -> string ("true"/"false") -> Active cooldown from escalating dismissals -> P3+ campaign exclusion filter -> YES

**5.5 Sync Configuration** -- Table:
| Parameter | Value | Notes |
- Sync Mode: Upsert (update existing + create new) | Primary mode Hightouch supports for CleverTap
- Identifier Field: user_id mapped to CleverTap Identity | Must be unique per user
- Sync Schedule: Every 30 minutes | Balance between freshness and API cost. Start with hourly, increase if needed.
- Change Detection: Incremental (Hightouch diff) | Only changed rows are synced -- reduces API calls
- New Profiles: Auto-created if Identity not found in CleverTap | No manual profile creation needed
- Error Handling: Hightouch retries failed records 3x, then flags as error | Monitor via Hightouch dashboard
- Alerting: Slack notification on sync failure | Configure Slack channel for CRM team

**5.6 Sync Monitoring** -- KPIs to track:
| Metric | Target | Alert | Source |
- Sync success rate: >99% | <95% = RED, investigate | Hightouch dashboard
- Sync latency (time from BQ update to CT update): <45 minutes | >60 minutes = AMBER | Hightouch sync logs
- Records synced per run: Track trend | Sudden spike or drop = investigate | Hightouch dashboard
- API errors: 0 | Any error = investigate | Hightouch error log
- Profile match rate: >99% (synced records found in CleverTap) | <95% = identity mismatch issue | Hightouch sync report

**5.7 Phase 2+ Event Sync (Future)** -- Brief forward-looking section:
- Phase 1 syncs USER PROFILES only (upsert mode)
- Phase 2-3 may add EVENT SYNC: BigQuery trigger_events_for_clevertap -> CleverTap events (insert mode)
- Event sync requires a truly unique primary key (event_id, not user_id which repeats)
- Event sync is INSERT-only (events are immutable once created)
- This enables BigQuery-computed triggers to fire as CleverTap events without building custom API integrations

**5.8 Implementation Checklist** -- Ordered steps for Alvaro:
1. Create BigQuery view: bit2me_lifecycle.user_profiles_for_clevertap (SQL above)
2. Create BigQuery service account with read access to bit2me_lifecycle
3. Get CleverTap Account ID and Passcode from Katy
4. Sign up for Hightouch Business tier
5. Connect BigQuery as source (upload service account JSON key)
6. Connect CleverTap as destination (Account ID, Passcode, Region: EU)
7. Create Model: select the user_profiles_for_clevertap view
8. Create Sync: map fields per the Field Mapping Table above
9. Set schedule: every 30 minutes
10. Run initial full sync (expect ~1.2M records minus 600K excluded = ~600K profiles)
11. Verify: check 10 random users in CleverTap dashboard to confirm properties match BigQuery
12. Configure Slack alerting for sync failures
13. Document Hightouch login credentials in team password manager
  </action>
  <verify>
    <automated>python3 -c "
import os
path = 'C:/Users/ferra/OneDrive/Desktop/.planning/phases/01-foundation-safety-architecture/playbook-section-hightouch.md'
content = open(path).read()
checks = [
    ('user_profiles_for_clevertap' in content, 'Has BigQuery view name'),
    ('lifecycle_stage' in content, 'Has lifecycle_stage field'),
    ('health_score' in content, 'Has health_score field'),
    ('notification_fatigue_score' in content, 'Has fatigue score field'),
    ('suppression_flags' in content, 'Has suppression flags field'),
    ('Upsert' in content or 'upsert' in content, 'Has upsert sync mode'),
    ('30 minutes' in content or '30 min' in content, 'Has sync schedule'),
    ('Account ID' in content and 'Passcode' in content, 'Has credential requirements'),
    ('CREATE OR REPLACE VIEW' in content, 'Has BigQuery SQL'),
    ('consent_marketing_push' in content, 'Has consent fields in mapping'),
    (content.count('|') > 30, 'Has multiple tables'),
    ('Slack' in content, 'Has alerting configuration'),
]
passed = sum(1 for c, _ in checks if c)
for c, label in checks:
    status = 'PASS' if c else 'FAIL'
    print(f'  {status}: {label}')
print(f'Result: {passed}/{len(checks)} checks passed')
assert passed >= 10, f'Only {passed}/12 checks passed'
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Section contains heading "## 5. Hightouch Reverse ETL Integration Design"
    - Setup Requirements table with 5 items (Account ID, Passcode, Region, Service Account, Hightouch Account) with owners
    - Full BigQuery SQL for user_profiles_for_clevertap view with all 17 fields
    - Field Mapping table with 17 rows, each with BigQuery column, CleverTap property, type, description, usage, criticality
    - Sync Configuration table specifying: Upsert mode, user_id as identifier, 30-minute schedule, incremental change detection
    - Sync Monitoring table with 5 KPIs and alert thresholds
    - Phase 2+ Event Sync section documenting future expansion
    - Implementation Checklist with 13 ordered steps for Alvaro
    - Statement that EXCLUDED users are filtered out of the view (~600K reduction)
  </acceptance_criteria>
  <done>Hightouch Reverse ETL section is complete with credentials, BigQuery source SQL, field mapping, sync config, monitoring KPIs, and implementation checklist. Alvaro can set up the full BigQuery-to-CleverTap pipeline from this document alone.</done>
</task>

</tasks>

<verification>
1. All three playbook sections exist as separate Markdown files in the phase directory
2. Suppression section contains all 6 subsections (3.1-3.6)
3. Event Schema section contains all 8 subsections (4.1-4.8)
4. Hightouch section contains all 8 subsections (5.1-5.8)
5. Cross-references: suppression_flags in Section 3 appears in Hightouch field mapping (Section 5); events in Section 4 map to triggers that reference fatigue from Section 2 and suppression from Section 3
6. No TBD values except C8 user count (pending Alvaro definition)
7. All sections specify owners (Katy, Alvaro, Diego) for actionable items
</verification>

<success_criteria>
- FOUND-03 fully addressed: Suppression system covers C8 whale list, quiet hours by timezone, opt-out handling per channel, escalating dismissal cooldowns with 5 levels
- FOUND-04 fully addressed: Event schema defines SDK events (7), Backend Upload events (6), Cloud Function events (2), with property types, API constraints, reserved names, and event-to-trigger mapping
- FOUND-05 fully addressed: Hightouch design specifies credentials, BigQuery SQL, field mapping (17 fields), sync mode/schedule, monitoring, and 13-step implementation checklist
- All three documents are self-contained and cross-reference each other and Plan 01 sections where relevant
</success_criteria>

<output>
After completion, create .planning/phases/01-foundation-safety-architecture/01-02-SUMMARY.md
</output>
